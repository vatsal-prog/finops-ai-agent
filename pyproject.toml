[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "finops-ai-agent"
version = "0.5.0"
description = "FinOps Agent — cloud cost optimization with custom MCP tools"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "FinOps Agent Contributors" }]
dependencies = [
  "mcp>=1.0",
  "pydantic>=2.0",
  "httpx>=0.27",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
finops-mcp = "finops_agent.mcp_server:main"
finops-agent = "finops_agent.cli:main"
finops-dashboard = "finops_agent.web.app:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
finops_agent = ["web/static/*", "web/templates/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
